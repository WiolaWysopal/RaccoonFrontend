import React from "react";
import styles from "./StyledButton.module.css"; // Import CSS Modules

const StyledButton = () => {
  return <button className={styles.button}>Click on me</button>;
};

export default StyledButton;
